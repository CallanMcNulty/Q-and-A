import { numberOfAnswers } from 'q-and-a/helpers/number-of-answers';
import { module, test } from 'qunit';

module('Unit | Helper | number of answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberOfAnswers([42]);
  assert.ok(result);
});
