import { categoryFormat } from 'q-and-a/helpers/category-format';
import { module, test } from 'qunit';

module('Unit | Helper | category format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryFormat([42]);
  assert.ok(result);
});
